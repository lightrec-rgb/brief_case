<h1 class="text-xl font-semibold mb-4">New <%= @card.kind %></h1>
<%= render "form" %>
<p class="mt-3">
  <%= link_to "Back", entries_path(subject_id: params[:subject_id] || @card&.subject_id), class: "underline" %>
</p>
